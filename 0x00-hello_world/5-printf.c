#include <stdio.h>

/**
 * main - Format and print data
 * Return: Prints "with proper grammar, but the outcome is a piece of art,\n"
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
